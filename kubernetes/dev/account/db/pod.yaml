apiVersion: v1
kind: Pod
metadata:
  name: account-db
  namespace: microshop
  labels:
    db: account
spec:
  containers:
    - name: postgres
      image: postgres:15.0
      env:
        - name: POSTGRES_DB
          value: account
        - name: POSTGRES_USER
          value: nihilus
        - name: POSTGRES_PASSWORD
          value: nihilus
      ports:
        - containerPort: 5432
      volumeMounts:
        - name: init-db
          mountPath: "/docker-entrypoint-initdb.d"
          readOnly: true
  volumes:
    - name: init-db
      configMap:
        name: account-init-db
        items:
          - key: "init_db.sql"
            path: "init_db.sql"
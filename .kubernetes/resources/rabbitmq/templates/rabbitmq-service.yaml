apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: solidvessel
  labels:
    app: rabbitmq
spec:
  selector:
    app: rabbitmq
  ports:
  - name: amqp
    port: 5672
    targetPort: 5672
  - name: manager
    port: 30672
    targetPort: 15672
  type: NodePort
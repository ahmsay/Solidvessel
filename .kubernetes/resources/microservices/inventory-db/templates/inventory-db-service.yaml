apiVersion: v1
kind: Service
metadata:
  name: account-db
  namespace: solidvessel
  labels:
    app: account-db
spec:
  selector:
    app: account-db
  ports:
  - name: account-db
    port: 5432
    targetPort: 5432
    nodePort: 32002
  type: NodePort
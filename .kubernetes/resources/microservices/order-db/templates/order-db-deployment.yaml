apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-db
  namespace: solidvessel
  labels:
    app: order-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-db
  revisionHistoryLimit: 2
  template:
    metadata:
      labels:
        app: order-db
    spec:
      containers:
      - name: order-db
        image: postgres:17.6
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: order_db
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: order-secret
              key: DB_USERNAME
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: order-secret
              key: DB_PASSWORD
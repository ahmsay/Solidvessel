apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: solidvessel
  labels:
    app: keycloak
spec:
  selector:
    app: keycloak
  ports:
  - name: keycloak
    port: 8080
    targetPort: 8080
    nodePort: 30080
  type: NodePort
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory
  namespace: solidvessel
spec:
  replicas: 15
  template:
    spec:
      containers:
      - name: inventory-app
        args: [ "--spring.profiles.active=prod" ]

apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory
  namespace: solidvessel
spec:
  replicas: 4
  template:
    spec:
      containers:
      - name: inventory-app
        args: [ "--spring.profiles.active=prod" ]

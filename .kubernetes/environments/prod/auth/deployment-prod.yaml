apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
  namespace: microshop
spec:
  template:
    spec:
      containers:
      - name: auth-app
        args: [ "--spring.profiles.active=prod" ]
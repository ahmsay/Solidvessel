server.port: 8083

spring:
  application.name: order-service
  cloud.config.uri: http://config-server/

eureka.instance.lease-renewal-interval-in-seconds: 5

logging.level.org.springframework.retry: trace
rootProject.name = 'solidvessel'

include ':account'
include ':auth'
include ':inventory'
include ':order'
include ':payment'
include ':shared'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven { url "https://plugins.gradle.org/m2" }
        maven { url "https://repo.spring.io/milestone" }
        maven { url "https://repo.maven.apache.org/maven2" }
    }
}
